function Init()
	playerCam = CurrentScene.Camera;
end

function Update(delta)

	--local moveRate = delta*30.
	--local turnRate = delta*50.
	--
	--if Input:IsKeyPressed(87) then --W
	--	this.Transform.Translation.X = this.Transform.Translation.X + this.Transform:GetForward().X * moveRate
	--	this.Transform.Translation.Y = this.Transform.Translation.Y + this.Transform:GetForward().Y * moveRate
	--	this.Transform.Translation.Z = this.Transform.Translation.Z + this.Transform:GetForward().Z * moveRate
	--end
	--
	--if Input:IsKeyPressed(83) then --S
	--	this.Transform.Translation.X = this.Transform.Translation.X - this.Transform:GetForward().X * moveRate
	--	this.Transform.Translation.Y = this.Transform.Translation.Y - this.Transform:GetForward().Y * moveRate
	--	this.Transform.Translation.Z = this.Transform.Translation.Z - this.Transform:GetForward().Z * moveRate
	--end
	--
	--if Input:IsKeyPressed(65) then --A
	--	this.Transform.Rotation.Y = this.Transform.Rotation.Y - turnRate
	--end
	--
	--if Input:IsKeyPressed(68) then --D
	--	this.Transform.Rotation.Y = this.Transform.Rotation.Y + turnRate
	--end


end